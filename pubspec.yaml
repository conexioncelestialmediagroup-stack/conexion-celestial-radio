name: conexion_celestial_radio
description: Radio Cristiana Conexión Celestial
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  just_audio: ^0.9.36
  just_audio_background: ^0.0.7
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.5
  connectivity_plus: ^5.0.2
  flutter_local_notifications: ^17.0.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
